id = "mvl"
name = "MVL"
version = "0.0.1"
schema_version = 1
description = "MVL (Maximum Verifiable Language) — syntax highlighting and editor support"
authors = ["MVL Team <https://github.com/LAB271>"]
repository = "https://github.com/LAB271/mvl_language"

# NOTE: Zed requires grammar.js at the root of the grammar repository.
# Until tree-sitter-mvl is a standalone repo, this points to the monorepo.
# For dev install, Zed will attempt to fetch and compile from this commit.
[grammars.mvl]
repository = "https://github.com/LAB271/tree-sitter-mvl"
commit = "ecb7111"
